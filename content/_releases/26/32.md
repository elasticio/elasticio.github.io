---
title: Product Update - v26.32
description: Details of the product update v26.32 from the 5th of August 2026.
layout: rel
version: "26.32"
section: Product Updates
category: product-updates
releaseDate: 2026-08-05
redirect_from:
  - /releases/26.32.html
---

## New Features
### Flow Limits for Ordinary and Long-Running Flows
Introduced new quota types to limit the number of ordinary and long-running (real-time) flows in workspaces and contracts. The new limits are:
* `per_workspace_ordinary_flow_count_limit`
* `per_workspace_long_running_flow_count_limit`
* `per_contract_ordinary_flow_count_limit`
* `per_contract_long_running_flow_count_limit`

### Block Login via Email and Password for SMG Tenant
Added the ability to hide/block standard email and password login for the SMG tenant. A new `hideLogin` parameter has been added to `samlproviders.config` and `oidcproviders.config` to support this behavior.

### Revitalize Mixpanel Connection
Revitalized the Mixpanel connection to the platform to ensure proper tracking and event delivery.

### Notify Users on Role Updates
Users will now receive an email notification whenever their role has been updated.

### Register from Login Page
Added the option for users to register directly from the Login page.

## Fixed Bugs
*   **FIXED** an issue where `DELETE /v2/flows/subscription?workspace_id=workspace_id` did not delete the `TASK_OPERATIONAL_ERROR` subscription.
*   **FIXED** a data aggregation issue in Bran's `events_v2` table in ClickHouse.
*   **FIXED** a bug where the Editor did not preserve the flattening behavior in complex JSONata expressions.
*   **FIXED** a minified React error #31 that occurred when clicking the Two-factor authentication button when the tenant setting `featureFlags.enabledTotp` was set to `false`.
*   **FIXED** an issue where the component pusher did not send the `repo-new-version-in-workspace-flows` email/notification after pushing a new component version.
*   **FIXED** redirection for the "Sign in with Google" functionality when the OIDC provider connected to a tenant had the `autoCreateUser=true` attribute.
